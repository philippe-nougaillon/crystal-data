<p id="notice"><%= notice %></p>

<h1>Historique</h1>

<table class="table">
  <thead>
    <tr>
      <th>Date</th>
      <th>Colonne</th>
      <th>Utilisateur</th>
      <th>Modifications</th>
    </tr>
  </thead>

  <tbody>
    <% @logs.each do |log| %>
      <tr>
        <td><%= l(log.created_at) %></td>
        <td><%= log.field.name %></td>
        <td><%= log.user.name %></td>
        <td><%= log.message %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

